import React from 'react'

const Loading = () => {

    // Simple loading that is showed while we are fetching the data from the API

    return (
        <div>
            LOADING
        </div>
    )
}

export default Loading
